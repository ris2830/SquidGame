// 1. Wir holen uns die "Express"-Werkzeugkiste, die wir gerade installiert haben.
const express = require('express');

// 2. Wir erstellen unsere App, unseren Kellner.
const app = express();
const port = 3000; // Wir sagen dem Kellner, er soll an Tisch/Tür Nummer 3000 warten.

// 3. Wir sagen dem Kellner, wo er die Speisekarte (Webseite) findet.
// 'public' ist der Ordner mit der index.html, style.css etc.
// '..' bedeutet 'gehe einen Ordner nach oben' (von /server nach /spy-game)
app.use(express.static('../public'));

// 4. Wir sagen dem Kellner, er soll jetzt anfangen zu lauschen.
app.listen(port, () => {
  console.log(`Server lauscht auf Port ${port}`);
});